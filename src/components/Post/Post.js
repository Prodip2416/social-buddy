import React from 'react';
import Button from '@material-ui/core/Button';

const Post = () => {
    return (
        <div>
            <br />
            <Button variant="contained" color="primary" p={4} >
                Hello World
            </Button>                
        </div>
    );
};

export default Post;